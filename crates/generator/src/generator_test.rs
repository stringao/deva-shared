#[cfg(test)]
mod tests {
    use crate::Generator;

    #[test]
    fn test_generator_new() {
        let gen = Generator::new();
        assert!(true); // Basic construction test
    }

    #[test]
    fn test_generator_has_templates() {
        let gen = Generator::new();
        // Generator should be able to generate
        assert!(true);
    }
}